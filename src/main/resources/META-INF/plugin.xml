<idea-plugin>
    <id>io.genai.php</id>
    <name>GenAI PHP Portable</name>
    <version>0.2.15</version>
    <vendor url="https://genai.io.vn/" email="contact@genai.io.vn">GenAI</vendor>

    <description><![CDATA[
        <p><b>Write &amp; run PHP code in IntelliJ IDEA Community for free.</b> PHP Portable
        is a lightweight, feature-rich way to add PHP support to the free IDE — no PhpStorm
        license and no system-wide PHP install required.</p>
        Minimal PHP support for IntelliJ IDEA Community (which does not bundle the
        paid PhpStorm language plugin):
        <ul>
          <li><b>Portable PHP SDK</b> — download a portable PHP interpreter from
              inside the IDE (like the JDK downloader); no system install required.</li>
          <li><b>Run PHP script</b> — a run configuration that executes a .php file
              with the selected PHP SDK or an explicit interpreter path.</li>
          <li><b>Syntax highlighting</b> — lightweight, lexer-based highlighting for
              .php files (tags, keywords, strings, comments, variables, numbers).</li>
        </ul>
        This is an MVP; semantic features (completion, go-to-def, inspections) are a
        later step, best delivered by wrapping a PHP language server via LSP4IJ.
    ]]></description>

    <change-notes><![CDATA[
        <b>0.2.15</b>
        <ul>
          <li>The <code>.php</code> file icon is now the official PhpStorm PHP file icon
              (from the JetBrains PHP plugin, Apache 2.0, copyright header preserved), so
              files look exactly like they do in PhpStorm.</li>
        </ul>
        <b>0.2.14</b>
        <ul>
          <li>The <code>.php</code> file type no longer sets a custom icon — files render with
              the IDE's default file icon.</li>
        </ul>
        <b>0.2.13</b>
        <ul>
          <li><b>Fixed: remaining red tabs/project-tree marks on files using protobuf
              accessors or env-variant config classes.</b> Those marks come from RAW
              ERROR-severity diagnostics (Wolf marks), which client-side filtering cannot
              touch. <code>worse.missing_member</code> — the provider behind both the
              protobuf <code>__call</code> accessor and the variant-class constant false
              positives — is now disabled by default at the source. All remaining
              ERROR-severity diagnostics are gone, so no file gets marked. Set
              <code>language_server.diagnostic_ignore_codes</code> yourself to re-enable
              (the plugin's protobuf/variant class filtering then still narrows the noise).</li>
        </ul>
        <b>0.2.12</b>
        <ul>
          <li><b>No more protobuf false positives and style noise on legacy codebases.</b>
              <ul>
                <li>A stub for the runtime-provided <code>\ProtobufMessage</code> base class is
                    installed with the PHP stubs, so inherited methods
                    (<code>parseFromString</code>, <code>serializeToString</code>, …) resolve
                    instead of being reported as missing.</li>
                <li>Generated protobuf message accessors (<code>getIdKeys</code>, …) are
                    provided by the runtime's <code>__call</code> magic — the remaining
                    "Method … does not exist" findings on <code>extends \ProtobufMessage</code>
                    classes are now suppressed (the class list ships with the generated class
                    map).</li>
                <li>Style diagnostics that are pure noise on legacy code —
                    <code>worse.docblock_missing_param</code>,
                    <code>worse.docblock_missing_return_type</code>,
                    <code>worse.missing_return_type</code>,
                    <code>worse.undefined_variable</code> — are disabled by default via the
                    maintained Phpactor config. Set
                    <code>language_server.diagnostic_ignore_codes</code> yourself to
                    override.</li>
              </ul></li>
        </ul>
        <b>0.2.11</b>
        <ul>
          <li><b>Fixed: diagnostics pipeline crashed with LSP4IJ 0.21.0</b> — it bundles
              lsp4j 1.0.0, where <code>Diagnostic.getMessage()</code> returns
              <code>Either&lt;String, MarkupContent&gt;</code> instead of <code>String</code>.
              The plugin's filter (compiled against the older signature) threw
              <code>NoSuchMethodError</code>, killing LSP4IJ's whole diagnostics pipeline —
              stale editor marks, tab underlines and project-tree error badges could never be
              cleared. Message access is now version-proof (reflective) and any failure
              degrades to "no filtering" instead of breaking the pipeline.</li>
          <li><b>PHP file icon</b> is now the classic PHP logo (ellipse + php) instead of the
              custom monospace mark.</li>
        </ul>
        <b>0.2.10</b>
        <ul>
          <li><b>Fixed: false "Class not found" still marked tabs and the project tree red.</b>
              Editor and Problems view were clean, but file tabs and the tree kept the red
              marks — those come from Phpactor's diagnostics pipeline, which runs in a
              configuration context that never receives the plugin's LSP settings. The plugin
              now maintains Phpactor's global config file (~/.config/phpactor/phpactor.json)
              pointing at a STATIC index that only the plugin's offline build writes, while
              each server session works on its own live copy (a running session rewrites
              index records and corrupts any index it shares). Diagnostics now resolve
              project classes correctly at the source: no false findings, no red marks
              anywhere.</li>
        </ul>
        <b>0.2.9</b>
        <ul>
          <li><b>Fixed: corrupted index caused recurring "Class not found".</b> The index warm-up
              ran concurrently with the language server, and Phpactor's on-disk index has no
              file locking — the two writers overwrote each other's records (classes ended up
              indexed with an empty file path, which re-broke diagnostics, editor marks and
              the project tree). The warm-up now completes synchronously before the server
              process starts, and the index is versioned anew (one clean rebuild).</li>
        </ul>
        <b>0.2.8</b>
        <ul>
          <li><b>Fixed: false "Class not found" still shown in the editor.</b> LSP4IJ renders
              editor underlines through a highlighting pass that bypasses the inspection hook
              used by 0.2.7's filter — the suppression now also hooks
              <code>createAnnotation</code>, the common funnel of all diagnostic rendering
              paths.</li>
        </ul>
        <b>0.2.7</b>
        <ul>
          <li><b>Fixed: mass "Class X not found" false positives on non-Composer projects.</b>
              Phpactor's diagnostics only resolve classes through a Composer autoloader (its
              fallback scanner follows no symlinks and requires file name == class name). The
              plugin now generates a Composer-compatible class map at every server start
              (~0.5s) and feeds it to Phpactor — plus filters the remaining false "not found"
              findings for classes that verifiably exist in the project. Real Composer
              projects are untouched.</li>
          <li><b>Fixed: the index warm-up was a silent no-op</b> — Phpactor's CLI rejects the
              nested JSON config format; it now sends flat keys, so the offline index build
              actually runs.</li>
        </ul>
        <b>0.2.6</b>
        <ul>
          <li><b>swoole_system / Swoole specialization.</b> The Swoole extension's legacy
              runtime aliases (<code>swoole_table</code>, <code>swoole_server</code>,
              <code>Co\*</code>, …) are registered with <code>class_alias()</code> and are
              invisible to static analysis. The plugin now generates stub declarations for
              them (one file per namespace, next to the PHP stubs), so go-to-definition,
              completion and constant resolution work on legacy Swoole code, and the
              remaining false "Class … not found" diagnostics are filtered client-side for
              exactly those names.</li>
        </ul>
        <b>0.2.5</b>
        <ul>
          <li><b>Better protobuf support.</b> Diagnostics are suppressed in generated
              <code>pb_proto_*.php</code> files (they only produced unfixable style noise), and
              the index size limit was raised so large generated files stay navigable.</li>
          <li><b>Framework-ready indexing.</b> The plugin now warms up Phpactor's index once per
              server start (offline <code>index:build</code>), so framework classes resolve
              immediately after opening a project instead of waiting for file events.</li>
        </ul>
        <b>0.2.4</b>
        <ul>
          <li><b>Fixed: Find Usages / "who calls this method" popup did nothing.</b> Clicking a
              method declaration with Cmd+Click / Ctrl+Click never listed callers and
              Edit ▸ Find Usages found nothing. Root cause: LSP4IJ's target-element evaluator is
              registered with an empty language key, so the platform never consults it and a
              reference-free PSI like ours has no way to resolve the caret target. The plugin now
              registers its own evaluator for the PHP language, wiring the caret identifier into
              LSP4IJ's <code>textDocument/references</code> pipeline.</li>
        </ul>
        <b>0.2.3</b>
        <ul>
          <li><b>Fixed: no completion / wrong "Class not found" errors in symlink-based
              workspaces.</b> Projects whose root is a container of symlinked checkouts (common
              with monorepo-style setups) now index correctly — Phpactor's indexer follows
              symlinks, and the on-disk index is versioned so the broken one is rebuilt once
              automatically.</li>
          <li><b>Fixed: editor errors after toggling the LSP4IJ plugin.</b> The PHP language and
              its token types are now attached to this plugin's main descriptor, so disabling or
              reloading LSP4IJ no longer breaks highlighting with
              "Trying to access element type from unloaded plugin" errors.</li>
        </ul>
        <b>0.2.2</b>
        <ul>
          <li>Added a plugin icon (shown in the Plugins list and on the Marketplace).</li>
        </ul>
        <b>0.2.1</b>
        <ul>
          <li>Refreshed the file/SDK icon to a cleaner monospace <b>php</b> mark.</li>
        </ul>
        <b>0.2.0</b>
        <ul>
          <li><b>PHP code intelligence.</b> Completion, go-to-definition, hover documentation
              and error highlighting — powered by a local PHP language server (Phpactor) that
              runs on the portable PHP interpreter. Fully offline; no Node.js or system install.
              Requires the free <b>LSP4IJ</b> plugin. Active on IDEs without built-in PHP (IDEA
              Community, GoLand, …); on PhpStorm the native PHP support is used instead.</li>
          <li><b>One-click setup.</b> A banner on your first <code>.php</code> file offers
              <b>Enable code intelligence</b>: it downloads Phpactor automatically and takes you
              straight to the Plugins screen to install LSP4IJ — no hunting for what's needed.</li>
          <li><b>Jump into PHP built-ins.</b> Go-to-definition works on standard PHP functions
              and classes (<code>\DateTime</code>, <code>json_encode</code>, …) via bundled stubs.</li>
          <li><b>Run Composer with portable PHP.</b> Right-click ▸ <b>Run Composer (Portable
              PHP)</b> to run install / update / dump-autoload / require and more in a console —
              no system PHP or Composer required.</li>
        </ul>
        <b>0.1.3</b>
        <ul>
          <li>Forward compatibility: dropped an internal-only platform API and the
              platform APIs scheduled for removal in newer IDE builds (verified with the
              JetBrains Plugin Verifier). No functional changes.</li>
        </ul>
        <b>0.1.2</b>
        <ul>
          <li>Maintenance: replaced an internal-only platform API with its public
              equivalent and updated deprecated icon-loading calls, for forward
              compatibility with newer IDE builds. No functional changes.</li>
        </ul>
        <b>0.1.1</b>
        <ul>
          <li><b>Works alongside PhpStorm &amp; IDEA Ultimate.</b> No longer clashes with the
              bundled PHP plugin: the editor language support quietly steps aside where PHP is
              already supported, while the portable interpreter, run action, and setup tools
              keep working in every IDE.</li>
          <li><b>Batteries-included PHP.</b> Downloaded interpreters now bundle ~65 extensions
              (intl, gd, soap, curl, zip, sodium, mbstring, and more), so
              <code>composer install</code> and real-world projects run without hitting a
              missing-extension error.</li>
          <li><b>Clearer run action.</b> The run configuration is now named
              <b>"PHP Script (Portable)"</b> so it's easy to tell apart from PhpStorm's
              built-in "PHP Script".</li>
          <li><b>Clearer settings.</b> The interpreter settings page is now
              <b>Settings ▸ Languages &amp; Frameworks ▸ Portable PHP</b>, so it no longer
              looks like a duplicate of PhpStorm's own PHP settings.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>

    <!-- Optional: PHP code intelligence (completion / navigation / errors) via LSP4IJ +
         Phpactor. Loads only when LSP4IJ is installed, so the core plugin (run a .php file,
         highlighting, portable interpreters) stays dependency-free and works on every IDE. -->
    <depends optional="true" config-file="lsp.xml">com.redhat.devtools.lsp4ij</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- 1. Portable PHP interpreter type (managed via our own UI, not the SDK combos) -->
        <sdkType implementation="io.genai.php.sdk.PhpSdkType"/>

        <!-- 3. File type + syntax highlighting.
             NOTE: no `extensions=` here on purpose. We must NOT statically claim .php, or we
             would clash with the official PHP plugin on PhpStorm / IDEA Ultimate. Instead the
             .php* extensions are bound at runtime, and only where no official PHP support
             exists (IDEA Community) — see io.genai.php.lang.PhpLanguageActivation.
             The language id is "PhpPortable" (unique) rather than "PHP" for the same reason. -->
        <fileType
            name="PHP File"
            language="PhpPortable"
            implementationClass="io.genai.php.lang.PhpFileType"
            fieldName="INSTANCE"/>
        <lang.syntaxHighlighterFactory
            language="PhpPortable"
            implementationClass="io.genai.php.lang.PhpSyntaxHighlighterFactory"/>
        <!-- Flat PSI (one leaf per lexer token) so the editor has word-level structure —
             fixes whole-file navigation underlines. Semantics still come from Phpactor/LSP. -->
        <lang.parserDefinition
            language="PhpPortable"
            implementationClass="io.genai.php.lang.PhpParserDefinition"/>

        <!-- Bind .php* to our file type at startup, Community-only (see class docs). -->
        <postStartupActivity implementation="io.genai.php.lang.PhpLanguageActivation"/>

        <!-- Developer diagnostics, inert without -Dphp.portable.diagnose=true (see class docs). -->
        <postStartupActivity implementation="io.genai.php.diag.PhpDiagnosticsActivity"/>

        <!-- 2. Run a PHP script -->
        <configurationType
            implementation="io.genai.php.run.PhpRunConfigurationType"/>
        <!-- Right-click ▸ Run and gutter ▶ marker on .php files -->
        <runConfigurationProducer
            implementation="io.genai.php.run.PhpRunConfigurationProducer"/>

        <!-- Manage portable PHP interpreters: Settings ▸ Languages & Frameworks ▸ Portable PHP -->
        <applicationConfigurable
            parentId="language"
            id="io.genai.php.interpreters"
            displayName="Portable PHP"
            instance="io.genai.php.settings.PhpInterpretersConfigurable"/>

        <!-- Banner on .php files when no PHP interpreter is configured yet -->
        <editorNotificationProvider
            implementation="io.genai.php.notify.PhpSetupNotificationProvider"/>
        <!-- Banner guiding the user to enable code intelligence (LSP4IJ + Phpactor) once an
             interpreter exists — otherwise completion/navigation silently don't work. -->
        <editorNotificationProvider
            implementation="io.genai.php.notify.PhpCodeIntelligenceNotificationProvider"/>

        <!-- Status-bar PHP version switcher -->
        <statusBarWidgetFactory
            id="PhpPortable.VersionWidget"
            implementation="io.genai.php.statusbar.PhpStatusBarWidgetFactory"/>
    </extensions>

    <actions>
        <!-- Run a Composer command with the portable PHP: <php> composer.phar <args>. -->
        <action
            id="io.genai.php.RunComposer"
            class="io.genai.php.composer.RunComposerAction"
            text="Run Composer (Portable PHP)…"
            description="Run a Composer command using the portable PHP interpreter"
            icon="/icons/php.svg">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
